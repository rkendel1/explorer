openapi: 3.1.0
info:
  title: OpenAPI Fixture
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      responses:
        '200':
          description: OK
    post:
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            example:
              email: user@example.com
              name: Example User
      responses:
        '201':
          description: Created
          content:
            application/json:
              example:
                id: usr_123
